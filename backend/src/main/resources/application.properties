
# server.port=5054

# spring.application.name=PROJECT-MANAGEMENT

# spring.datasource.url=jdbc:mysql://localhost:3306/projectmanagment2
# spring.datasource.username=root
# spring.datasource.password=System
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# spring.jpa.hibernate.ddl-auto=update

# #email
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=k21370794@gmail.com
# spring.mail.password=pvwszerlfaqqisiq
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# razorpay.api.key=rzp_test_Rz6CHkHLpaBuLh
# razorpay.api.secret=SQ0aysahtxJnd8eZMh6mHeoD

# Server Configuration
server.port=${SERVER_PORT}
spring.application.name=${SPRING_APPLICATION_NAME}

# Database Configuration
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Razorpay Configuration
razorpay.api.key=${RAZORPAY_KEY}
razorpay.api.secret=${RAZORPAY_SECRET}